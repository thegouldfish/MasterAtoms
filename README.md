# Master Atoms



First release of Atoms for the Master System.

Currently missing the build pipeline as it requires cleaning up.

So as it stands the code will not compile, as the build system generates some of the header files required.

